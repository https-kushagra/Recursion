#include <stdio.h>
int sum(int n) 
  {
  
  if(n==0)
  {
    return 0;
  }
  else
  {
    return sum(n-1)+n;
       
  }
}
  int main()
  { int num;
   scanf("%d",&num);
int nat=sum(num);
printf("%d",nat);
return 0;
  }
    
