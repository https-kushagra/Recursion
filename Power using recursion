#include <stdio.h>
int power(int m,int n) 
  {
  
  if(n==0)
  {
    return 1;
  }
  else
  {
    return power(m,n-1)*m;
       
  }
}
  int main()
  { int num1,num2;
   scanf("%d %d",&num1,&num2);
int powr=power(num1,num2);
printf("%d",powr);
   return 0;
  }
    
